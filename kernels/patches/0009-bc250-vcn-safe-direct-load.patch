diff --git a/drivers/gpu/drm/amd/amdgpu/amdgpu.h b/drivers/gpu/drm/amd/amdgpu/amdgpu.h
index 0dd8e23..e138ebc 100644
--- a/drivers/gpu/drm/amd/amdgpu/amdgpu.h
+++ b/drivers/gpu/drm/amd/amdgpu/amdgpu.h
@@ -265,6 +265,7 @@ extern int amdgpu_umsch_mm_fwlog;
 
 extern int amdgpu_user_partt_mode;
 extern int amdgpu_agp;
+extern bool amdgpu_bc250_vcn;
 extern int amdgpu_rebar;
 
 extern int amdgpu_wbrf;
diff --git a/drivers/gpu/drm/amd/amdgpu/amdgpu_discovery.c b/drivers/gpu/drm/amd/amdgpu/amdgpu_discovery.c
index 5e83edb..f8ac809 100644
--- a/drivers/gpu/drm/amd/amdgpu/amdgpu_discovery.c
+++ b/drivers/gpu/drm/amd/amdgpu/amdgpu_discovery.c
@@ -2652,6 +2652,11 @@ static int amdgpu_discovery_set_mm_ip_blocks(struct amdgpu_device *adev)
 				amdgpu_device_ip_block_add(adev, &jpeg_v2_0_ip_block);
 			break;
 		case IP_VERSION(2, 0, 3):
+			if (amdgpu_bc250_vcn && adev->pdev->device == 0x13fe) {
+				amdgpu_device_ip_block_add(adev, &vcn_v2_0_ip_block);
+				if (!amdgpu_sriov_vf(adev))
+					amdgpu_device_ip_block_add(adev, &jpeg_v2_0_ip_block);
+			}
 			break;
 		case IP_VERSION(2, 5, 0):
 			amdgpu_device_ip_block_add(adev, &vcn_v2_5_ip_block);
diff --git a/drivers/gpu/drm/amd/amdgpu/amdgpu_drv.c b/drivers/gpu/drm/amd/amdgpu/amdgpu_drv.c
index ec4225c..142e461 100644
--- a/drivers/gpu/drm/amd/amdgpu/amdgpu_drv.c
+++ b/drivers/gpu/drm/amd/amdgpu/amdgpu_drv.c
@@ -241,6 +241,7 @@ int amdgpu_umsch_mm;
 int amdgpu_seamless = -1; /* auto */
 uint amdgpu_debug_mask;
 int amdgpu_agp = -1; /* auto */
+bool amdgpu_bc250_vcn;
 int amdgpu_wbrf = -1;
 int amdgpu_damage_clips = -1; /* auto */
 int amdgpu_umsch_mm_fwlog;
@@ -812,6 +813,15 @@ bool hws_gws_support;
 module_param_unsafe(hws_gws_support, bool, 0444);
 MODULE_PARM_DESC(hws_gws_support, "Assume MEC2 FW supports GWS barriers (false = rely on FW version check (Default), true = force supported)");
 
+/**
+ * DOC: bc250_vcn (bool)
+ * Opt in to registering the VCN 2.0/JPEG blocks on AMD BC-250 device 13fe.
+ * Disabled by default. Direct firmware experiments additionally require
+ * amdgpu.fw_load_type=0 on the test boot entry.
+ */
+module_param_named(bc250_vcn, amdgpu_bc250_vcn, bool, 0444);
+MODULE_PARM_DESC(bc250_vcn, "Register experimental BC-250 VCN 2.0 support");
+
 /**
  * DOC: queue_preemption_timeout_ms (int)
  * queue preemption timeout in ms (1 = Minimum, 9000 = default)
diff --git a/drivers/gpu/drm/amd/amdgpu/amdgpu_ucode.c b/drivers/gpu/drm/amd/amdgpu/amdgpu_ucode.c
index 6d9e96f..6747cf9 100644
--- a/drivers/gpu/drm/amd/amdgpu/amdgpu_ucode.c
+++ b/drivers/gpu/drm/amd/amdgpu/amdgpu_ucode.c
@@ -1359,6 +1359,7 @@ static const char *amdgpu_ucode_legacy_naming(struct amdgpu_device *adev, int bl
 		case IP_VERSION(2, 6, 0):
 			return "aldebaran_vcn";
 		case IP_VERSION(2, 0, 0):
+		case IP_VERSION(2, 0, 3):
 			return "navi10_vcn";
 		case IP_VERSION(2, 0, 2):
 			if (adev->asic_type == CHIP_NAVI12)
